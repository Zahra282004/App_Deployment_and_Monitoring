apiVersion: v1
kind: Service
metadata:
  name: db-service
  namespace: 3-tier-app
spec:
  type: ClusterIP   
  selector:
    app: db
  ports:
    - protocol: TCP
      port: 3306
      targetPort: 3306
